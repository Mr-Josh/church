# URL of the PHP API
VITE_API_URL=http://localhost:8000/api

# Public canonical URL used to generate robots.txt and sitemap.xml during production builds.
# Replace with the real HTTPS domain before deployment.
VITE_SITE_URL=https://votre-domaine.tld
